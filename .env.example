# Supabase (optional — app runs with mock data if unset)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Admin access (Option B) — comma-separated emails
ADMIN_EMAILS=admin@pickfist.example
